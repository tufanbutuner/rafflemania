# Postgres connection string. Payload's Postgres adapter needs this at build
# and runtime; see issue #72 for provisioning.
DATABASE_URI=postgresql://user:password@localhost:5432/rafflemania

# Used to sign and encrypt Payload auth tokens. Generate with:
#   openssl rand -hex 32
# Changing it invalidates every existing session and password-reset token.
PAYLOAD_SECRET=
